# 2026-06-23 Refactor History: Lyric Layout Components

## Scope

Continued Step 4 of the component migration plan: port the desktop lyric horizontal and vertical display shells into React.

## Completed

- Added `src/lyric-react/components/layout/LyricLineList.tsx`.
- Added `src/lyric-react/components/layout/LyricHorizontal.tsx`.
- Added `src/lyric-react/components/layout/LyricVertical.tsx`.
- Added `src/lyric-react/components/layout/index.ts`.
- Updated `src/lyric-react/App.tsx` to choose horizontal or vertical lyric rendering from MobX config.
- Updated `src/lyric-react/styles/index.css` with React equivalents for:
  - lyric line host layout,
  - horizontal and vertical writing modes,
  - active-line zoom classes,
  - ellipsis and font-weight flags,
  - line-mode and font-mode color transitions,
  - per-character gradient fill styles.

## Current Status

- React lyric layout can host the DOM nodes generated by the shared lyric player.
- The full legacy drag/resize/manual-scroll behavior is not yet migrated.
- No legacy Vue files were deleted in this batch.
- Legacy Vue SFC count remains 122.

## Validation

Passed for this batch:

- `npm run lint`
- `npm run build:renderer-lyric`
- `npm run build`

Notes:

- The lyric renderer still reports the existing Vite chunk-size warning.

## Next Plan

1. Port `ControlBar.vue` and replace toolbar controls with a dedicated React control bar.
2. Port resize/drag/manual lyric scroll behavior.
3. Port pause-hide, hover-hide, and audio visualization.
